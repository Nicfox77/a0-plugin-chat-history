List all stored chat contexts (main chat, subagents, background jobs) with
message counts and live status. Use first when you need a context id.

Input schema for tool_args:
~~~json
{"type": "object", "properties": {}, "additionalProperties": false}
~~~
